# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

#export PATH
unset USERNAME

#eval $(/home/linuxbrew/.linuxbrew/bin/brew shellenv)

#export PKG_CONFIG_PATH=/home/linuxbrew/.libnuxbrew/lib/pkgconfig:/usr/share/pkgconfig:/usr/lib64/pkgconfig
#source ~/perl5/perlbrew/etc/bashrc

